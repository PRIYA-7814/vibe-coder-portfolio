import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Alex Johnson - Portfolio | First-Year Engineering Student",
  description:
    "Portfolio website of Alex Johnson, a first-year engineering student passionate about software development and creating innovative solutions.",
  keywords: "portfolio, engineering student, software developer, web development, programming",
  authors: [{ name: "Alex Johnson" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
